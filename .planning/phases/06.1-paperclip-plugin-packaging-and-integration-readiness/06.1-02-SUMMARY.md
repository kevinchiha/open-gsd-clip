---
phase: 06.1-paperclip-plugin-packaging-and-integration-readiness
plan: 02
subsystem: plugin
tags: [paperclip, tsup, build, packaging, npm, esm]

requires:
  - phase: 06.1-paperclip-plugin-packaging-and-integration-readiness
    provides: Standalone manifest module and worker entry point from Plan 01
provides:
  - Multi-entry tsup build producing dist/manifest.js, dist/plugin/worker.js, dist/index.js
  - package.json with paperclipPlugin discovery fields for Paperclip plugin-loader
  - npm-publishable package with main, types, exports, files fields
affects: [plugin-publishing, paperclip-integration]

tech-stack:
  added: []
  patterns: [multi-entry tsup ESM build, paperclipPlugin package.json discovery field, code-splitting with shared chunks]

key-files:
  created:
    - tsup.config.ts
  modified:
    - package.json
    - src/notifications/preferences.test.ts

key-decisions:
  - "Multi-entry tsup config with map syntax to control output paths (plugin/worker produces dist/plugin/worker.js)"
  - "ESM-only format since package is type:module -- no CJS output needed"
  - "All runtime deps externalized to prevent bundling (execa, zod, js-yaml, pino, @paperclipai/shared)"
  - "splitting:true for shared chunks between entry points"
  - "prepublishOnly + prepare scripts ensure dist/ always present for npm publish and npm link"

patterns-established:
  - "paperclipPlugin field in package.json: { manifest, worker } paths for Paperclip plugin discovery"
  - "tsup entry map syntax: key = output path relative to dist/, value = source file"

requirements-completed: [PKG-04, PKG-05, PKG-07]

duration: 3min
completed: 2026-03-18
---

# Phase 06.1 Plan 02: Build Configuration and Package.json Plugin Fields Summary

**Multi-entry tsup ESM build with paperclipPlugin discovery fields, npm exports, and verified dist/ structure matching manifest entrypoints**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-18T19:40:29Z
- **Completed:** 2026-03-18T19:42:59Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Created tsup.config.ts with 3 entry points (manifest, plugin/worker, index) producing correct dist/ structure
- Updated package.json with paperclipPlugin manifest/worker paths, main, types, exports, files fields
- Added prepublishOnly and prepare scripts for reliable npm publish and npm link workflows
- Verified dist/manifest.js importable with correct default export (id: @open-gsd/clip)
- All 493 tests pass, TypeScript compiles clean, lint passes on modified files

## Task Commits

Each task was committed atomically:

1. **Task 1: Create tsup.config.ts and update package.json** - `a33b2d4` (feat)
2. **Task 2: Verify build output and full regression** - `c15c9da` (fix)

## Files Created/Modified
- `tsup.config.ts` - Multi-entry ESM build config with 3 entry points, code splitting, externals
- `package.json` - Added paperclipPlugin, main, types, exports, files, prepublishOnly, prepare
- `src/notifications/preferences.test.ts` - Removed unused NotificationPreference type import (deviation fix)

## Decisions Made
- Multi-entry tsup config uses map syntax where keys control output paths (`'plugin/worker'` -> `dist/plugin/worker.js`)
- ESM-only format (`format: ['esm']`) since the package is `"type": "module"` -- no CJS consumers expected
- All 5 runtime dependencies externalized to prevent bundling into dist/ (keeps files small, lets npm handle deps)
- `splitting: true` enables shared chunks between entry points for deduplication
- Both `prepublishOnly` (npm publish) and `prepare` (npm link) scripts run build to ensure dist/ is always present

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed unused NotificationPreference type import**
- **Found during:** Task 2 (TypeScript typecheck verification)
- **Issue:** Pre-existing unused type import in `src/notifications/preferences.test.ts` caused `tsc --noEmit` to fail with TS6133, blocking verification
- **Fix:** Removed the unused `type NotificationPreference` from the import statement, ran biome format to adjust line wrapping
- **Files modified:** src/notifications/preferences.test.ts
- **Verification:** `tsc --noEmit` passes clean, all 13 preferences tests still pass
- **Committed in:** c15c9da

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Trivial pre-existing issue that blocked verification. No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plugin fully packaged: manifest, worker, and barrel export all build to correct dist/ paths
- package.json has all fields needed for `npm install` by Paperclip
- Plugin is discoverable via `paperclipPlugin.manifest` and `paperclipPlugin.worker` fields
- Ready for npm publish or local `npm link` testing with Paperclip

## Self-Check: PASSED

All artifacts verified:
- tsup.config.ts: FOUND
- package.json: FOUND
- dist/manifest.js: FOUND
- dist/plugin/worker.js: FOUND
- dist/index.js: FOUND
- 06.1-02-SUMMARY.md: FOUND
- Commit a33b2d4: FOUND
- Commit c15c9da: FOUND

---
*Phase: 06.1-paperclip-plugin-packaging-and-integration-readiness*
*Completed: 2026-03-18*
